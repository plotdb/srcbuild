// Generated by LiveScript 1.6.0
var fs, watch, pug, stylus, lsc, base;
fs = require('fs');
watch = require('./watch');
pug = require('./ext/pug');
stylus = require('./ext/stylus');
lsc = require('./ext/lsc');
base = require('./ext/base');
module.exports = {
  base: base,
  lsp: function(opt){
    var base, adapters, watcher;
    opt == null && (opt = {});
    base = opt.base || 'web';
    adapters = [
      new lsc(import$({
        base: base
      }, opt)), new stylus(import$({
        base: base
      }, opt)), new pug(import$({
        base: base
      }, opt))
    ].map(function(it){
      return it.getAdapter();
    });
    return watcher = new watch(import$({
      adapters: adapters
    }, opt));
  }
};
function import$(obj, src){
  var own = {}.hasOwnProperty;
  for (var key in src) if (own.call(src, key)) obj[key] = src[key];
  return obj;
}