// Generated by LiveScript 1.6.0
var fs, watch, pug, stylus, lsc;
fs = require('fs');
watch = require('./watch');
pug = require('./ext/pug');
stylus = require('./ext/stylus');
lsc = require('./ext/lsc');
module.exports = function(opt){
  var base, adapters, watcher;
  opt == null && (opt = {});
  base = opt.base || 'web';
  adapters = [
    new pug({
      base: base
    }), new stylus({
      base: base
    }), new lsc({
      base: base
    })
  ].map(function(it){
    return it.getAdapter();
  });
  return watcher = new watch({
    adapters: adapters
  });
};