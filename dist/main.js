// Generated by LiveScript 1.6.0
var fs, srcbuild, srcwatch, aux, pugbuild, stylusbuild, lscbuild, base, builders;
fs = require('fs');
srcbuild = require('./srcbuild');
srcwatch = require('./srcwatch');
aux = require('./aux');
pugbuild = require("./ext/pug");
stylusbuild = require("./ext/stylus");
lscbuild = require("./ext/lsc");
base = 'web';
builders = [
  new pugbuild({
    base: base
  }), new stylusbuild({
    base: base
  }), new lscbuild({
    base: base
  })
].map(function(it){
  return it.getBuilder();
});
new srcwatch({
  builders: builders
});